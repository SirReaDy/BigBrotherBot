__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.sqlite
*.db
.coverage
htmlcov/

# --- a deployed bot's own files (examples/ keeps the committed templates) ---
/b3.yaml
/plugin_*.yaml
/plugins/

# --- internal working documents, not published ---
# The architecture rationale, the parity audit against classic B3, and the working list of what is
# left to build. They are how this project is planned and they stay ours; README.md is the public
# document and is written to stand on its own without them.
/DESIGN.md
/PARITY.md
/TODO.md
# --- generated documentation ---
# `docs/index.md` is README.md with its links rewritten for the site (tools/build_docs_index.py).
# Not committed: it would double every README diff, and a copy that can drift is the exact thing the
# generator exists to prevent. The docs workflow writes it before `mkdocs build`; run the tool
# yourself before `mkdocs serve`.
/docs/index.md
/site/
